class EulerPhi {
    private static int euler(int a, int b){
        int c = Math.min(a,b);
        for(int iter=a-1;iter>0;iter--) {
            if(a%iter==0) && (b%iter==0) {
                hcf = iter;
                break;
            }
        }
    }
}